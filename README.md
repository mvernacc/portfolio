# portfolio

A portfolio of my engineering work.

View the site at https://mvernacc.github.io/portfolio

To build and serve the docs locally, run

```bash
uv run zensical serve
```

The first time you run this, `uv` will set up a new virtual environment and install zensical and its dependencies.
